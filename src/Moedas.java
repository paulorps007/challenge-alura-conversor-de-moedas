public record Moedas(String base_code, String target_code,
                     String conversion_rate, String conversion_result){
// Onde: base_code= moeda principal,traget_code= moeda convertida, conversion_rate= moeda convertida, conversion_result= valor convertido da moeda

}
